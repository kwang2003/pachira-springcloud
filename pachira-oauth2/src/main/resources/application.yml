server:
  port: 8081
  
spring:
  application:
    name: pachira-oauth2
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        preferIpAddress: true
        instanceId: ${spring.application.name}@${spring.cloud.client.ip-address}:${server.port}